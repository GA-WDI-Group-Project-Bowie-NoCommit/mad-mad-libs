<!DOCTYPE html>
<html lang="en">
  <% include ../../partials/head.ejs %>
  <body>

    <header>
      <% include ../../partials/nav.ejs %>
    </header>
    <!-- session -->
      <% if (true) {%>
<!-- session.name -->
        <h3>Welcome <%=  %></h3>
        <form method="post" action="users/logout?_method=DELETE">
          <button>logout</button>
        </form>

        <div>
          <a href="/crud">View all CRUD</a>
        </div>
        <div>
          <a href="/crud/new">Create CRUD</a>
        </div>
        <div>
        <!-- has access to db items via crud[i].whatever -->
        </div>
      <% } else { %>

        <div>
          <a href="/users/new">Signup</a>
        </div>
        <div>
          <a href="/users/login">Log In</a>
        </div>

        <% } %>

    <% include ../../partials/footer.ejs %>
  </body>
</html>
